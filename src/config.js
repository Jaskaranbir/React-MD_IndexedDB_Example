const config = {
  collectionName: 'Customers',
  dbName: 'Company',
  dbVersion: 1
}

export default config

export const collectionName = config.collectionName
export const dbName = config.dbName
export const dbVersion = config.dbVersion
